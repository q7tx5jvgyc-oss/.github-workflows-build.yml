- name: Build
        run: |
          export THEOS=/home/runner/theos
          export PATH=$THEOS/bin:$PATH
          # التوجه إلى المسار الرئيسي للمستودع قبل تنفيذ الأمر
          cd $GITHUB_WORKSPACE
          make clean
          make
          make package
