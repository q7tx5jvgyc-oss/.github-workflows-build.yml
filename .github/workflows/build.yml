- name: Build Tweak
        run: |
          export THEOS=/home/runner/theos
          export PATH=$THEOS/bin:$PATH
          # التأكد من تحديث القوالب قبل البناء
          make clean || true
          make
          make package
